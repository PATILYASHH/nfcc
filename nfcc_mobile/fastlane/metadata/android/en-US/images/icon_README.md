# App icon

Place a **512×512 PNG** icon here as `icon.png`.
F-Droid and Fastlane read this file for the store listing.

The launcher icon that ships inside the APK lives under
`android/app/src/main/res/mipmap-*/ic_launcher.*` — that's separate.
